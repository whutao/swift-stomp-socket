import XCTest
@testable import swift_stomp_socket

final class swift_stomp_socketTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_stomp_socket().text, "Hello, World!")
    }
}
